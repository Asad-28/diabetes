from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='It is a project to determine if the paitent has a diabetes or not.',
    author='Asad',
    license='MIT',
)
